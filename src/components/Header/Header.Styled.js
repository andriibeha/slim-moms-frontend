import styled from '@emotion/styled';
import {
  typography,
  space,
  color,
  flexbox,
  border,
  layout,
} from 'styled-system';

export const HeaderStyled = styled('header')(
  typography,
  space,
  color,
  flexbox,
  border,
  layout
);
