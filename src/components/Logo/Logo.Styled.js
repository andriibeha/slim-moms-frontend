import styled from '@emotion/styled';
import { typography, space, color, flexbox, layout } from 'styled-system';

export const Box = styled('div')(flexbox, layout);
export const LogoStyled = styled('p')(typography, space, color);
export const Span = styled('span')(typography, color);
export const Img = styled('img')(layout);
